{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "functions": {
    "api/gemini.js": { "runtime": "edge" }
  },
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
